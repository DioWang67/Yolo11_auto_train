# Python 3.10 CI compatibility boundary for the API and pytest stacks.
# Keep these pins aligned with requirements-dev.txt. The remaining runtime
# dependencies retain their project constraints so pip can select Python 3.10
# compatible wheels.
fastapi==0.128.0
httpx==0.28.1
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-qt==4.5.0
pytest-timeout==2.4.0
starlette==0.50.0
