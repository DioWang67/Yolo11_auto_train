presets:
  YOLO訓練:
    # 使用 pipeline 中已註冊的任務名稱，避免「未知任務」警告
    - yolo_augmentation
    - dataset_splitter
    - yolo_train
    - yolo_evaluation
    - generate_report
    - dataset_lint
    - aug_preview
    - batch_inference

  顏色檢測:
    - color_inspection
    - color_verification
