presets:
  "YOLO: train and package":
    - dataset_splitter
    - yolo_train
    - artifact_bundle

  "YOLO: train and deploy":
    - yolo_augmentation
    - dataset_lint
    - dataset_splitter
    - yolo_train
    - position_validation
    - yolo_evaluation
    - generate_report
    - batch_inference
    - qc_summary
    - deploy

  "YOLO: train only":
    - dataset_splitter
    - yolo_train

  "YOLO: package existing run":
    - artifact_bundle

  "Anomalib: train and package":
    - anomalib_train
    - anomalib_package

  "Anomalib: train only":
    - anomalib_train

  "Anomalib: package existing run":
    - anomalib_package

  Data prep only:
    - format_conversion
    - dataset_lint
    - yolo_augmentation
    - dataset_splitter

  Inference smoke test:
    - batch_inference
    - qc_summary
